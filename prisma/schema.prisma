generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model UserCreated {
  id         String  @id @default(cuid())
  name       String
   email      String?
  perfil_url String?


  @@map("UserCreated")
}
